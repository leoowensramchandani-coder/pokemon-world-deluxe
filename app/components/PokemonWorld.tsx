"use client";
import { useEffect, useMemo, useState } from "react";
import Image from "next/image";
import { trainers } from "@/lib/trainers";
import type { Collections, PokemonCard, TrainerId } from "@/lib/types";
import TrainerCard from "./TrainerCard"; import ProfessorOak from "./ProfessorOak"; import CardSearch from "./CardSearch"; import CardScanner from "./CardScanner"; import Achievements from "./Achievements"; import FamilyLogin from "./FamilyLogin";

const empty: Collections = { papa: [], leo: [], remy: [] };
export default function PokemonWorld() {
  const [collections, setCollections] = useState<Collections>(empty); const [searchOpen, setSearchOpen] = useState(false); const [scannerOpen, setScannerOpen] = useState(false); const [activeBinder, setActiveBinder] = useState<TrainerId | null>(null); const [begun, setBegun] = useState(false); const [cloudMode, setCloudMode] = useState(false); const [accessToken, setAccessToken] = useState(""); const [notice, setNotice] = useState("");
  async function load() { const response = await fetch("/api/collections", { cache: "no-store" }); if (response.ok) setCollections(await response.json()); }
  useEffect(() => { fetch("/api/collections", { cache: "no-store" }).then((response) => response.json()).then(setCollections); fetch("/api/auth/config").then((response) => response.json()).then((body) => setCloudMode(body.cloudMode)); }, []);
  const total = useMemo(() => Object.values(collections).flat().reduce((sum, item) => sum + item.quantity, 0), [collections]); const canEdit = !cloudMode || Boolean(accessToken);
  async function addCard(card: PokemonCard, trainer: TrainerId) { if (!canEdit) { setNotice("A grown-up needs to use Family Sign In first."); return; } const response = await fetch("/api/collections", { method: "POST", headers: { "Content-Type": "application/json", ...(accessToken ? { Authorization: `Bearer ${accessToken}` } : {}) }, body: JSON.stringify({ trainer, card }) }); if (!response.ok) { setNotice("Your Trainer Pass expired. Please sign in again."); return; } await load(); }
  async function removeCard(cardId: string, trainer: TrainerId) { if (!canEdit) { setNotice("Family Sign In is needed to remove cards."); return; } await fetch("/api/collections", { method: "DELETE", headers: { "Content-Type": "application/json", ...(accessToken ? { Authorization: `Bearer ${accessToken}` } : {}) }, body: JSON.stringify({ trainer, cardId }) }); await load(); }
  const currentTrainer = trainers.find((item) => item.id === activeBinder);
  return <main className="site-shell"><div className="ambient ambient-one"/><div className="ambient ambient-two"/><div className="page-wrap"><header className="hero"><div><span className="hero-pill">⚡ Family Trainer HQ</span><h1>Pokémon World Deluxe</h1><p>Open the family binder, discover real cards, and collect together.</p></div><div className="hero-side"><div className="hero-stats"><div><strong>{total}</strong><span>Family Cards</span></div><div><strong>3</strong><span>Collections</span></div></div>{cloudMode && <FamilyLogin accessToken={accessToken} onLogin={(token) => { setAccessToken(token); setNotice("Family editing unlocked! 🎉"); }} onLogout={() => setAccessToken("")}/>}</div></header>{notice && <button className="notice-banner" onClick={() => setNotice("")}>{notice} <span>×</span></button>}<ProfessorOak totalCards={total} begun={begun} onBegin={() => { setBegun(true); setSearchOpen(true); }}/><section className="collection-section"><div className="section-heading"><div><p className="eyebrow">Choose your Trainer</p><h2>Family Collections</h2></div><div className="header-actions">{canEdit && <button className="secondary-button" onClick={() => setScannerOpen(true)}>📷 Scan Binder</button>}<button className="primary-button" onClick={() => setSearchOpen(true)}>🔎 Search Cards</button></div></div><div className="trainer-grid">{trainers.map((trainer) => <TrainerCard key={trainer.id} trainer={trainer} items={collections[trainer.id]} onOpen={setActiveBinder}/>)}</div></section><Achievements collections={collections}/><section className="toolbox"><div><p className="eyebrow">{canEdit ? "Next Mission" : "Visitor Mode"}</p><h2>{canEdit ? "Fill a binder page!" : "Explore the family collection!"}</h2><p>{canEdit ? "Photograph up to 18 cards, check the matches, and add them together." : "Visitors can browse every binder without changing any cards."}</p></div>{canEdit && <button className="primary-button" onClick={() => setScannerOpen(true)}>Open Scanner →</button>}</section><footer>Built together by the Pokémon World Deluxe family.</footer></div>{searchOpen && <CardSearch canEdit={canEdit} onAdd={addCard} onClose={() => setSearchOpen(false)}/>} {scannerOpen && canEdit && <CardScanner accessToken={accessToken} onAdd={addCard} onClose={() => setScannerOpen(false)}/>} {activeBinder && currentTrainer && <div className="modal-backdrop" role="dialog" aria-modal="true"><div className="binder-modal"><button className="close-button" onClick={() => setActiveBinder(null)}>×</button><p className="eyebrow">{currentTrainer.badge} {currentTrainer.title}</p><h2>{currentTrainer.name}&apos;s Binder</h2>{collections[activeBinder].length === 0 ? <div className="empty-binder"><span>📖</span><h3>This binder is ready!</h3><p>{canEdit ? "Search or scan to make the first catch." : "No cards have been added yet."}</p>{canEdit && <button className="primary-button" onClick={() => { setActiveBinder(null); setScannerOpen(true); }}>Scan Cards</button>}</div> : <div className="binder-grid">{collections[activeBinder].map((item) => <div key={item.card.id} className="binder-card">{item.quantity > 1 && <span className="copy-count">×{item.quantity}</span>}<Image src={item.card.images.small} alt={item.card.name} width={245} height={342}/><b>{item.card.name}</b><small>{item.quantity} {item.quantity === 1 ? "copy" : "copies"}</small>{canEdit && <button className="remove-button" onClick={() => removeCard(item.card.id, activeBinder)}>− Remove one</button>}</div>)}</div>}</div></div>}</main>;
}
